{
  "name": "volet-market-ecomirse",
  "private": true,
  "description": "Monorepo ildizi — skriptlar violet-server / my-violet-market ga yo‘naltiriladi",
  "scripts": {
    "seed": "npm run seed --prefix violet-server",
    "server": "npm start --prefix violet-server",
    "server:check": "npm run db:check --prefix violet-server"
  }
}
